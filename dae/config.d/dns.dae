# Use alidns for all DNS queries and fallback to googledns if pollution result detected.
dns {

  upstream {
    mosdns: 'udp+tcp://127.0.0.1:5335'
  }
  routing {
    request {
      fallback: mosdns
    }
  }
}
